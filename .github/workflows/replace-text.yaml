name: replace-text

# on push to main
on:
  workflow_dispatch:
    inputs:
      vcluster_name:
        description: "Override default vCluster name which matches the repo name (minus `-app`)"
        required: false
        default: ""
      base_domain:
        description: "Base domain used to create example host names"
        required: true
        default: "us.demo.dev"
  push:
    branches:
      - main
    paths:
      - 'helm-chart/Chart.yaml'
env:
  REPO_NAME: ${{ github.event.repository.name }}
  ORG_NAME: ${{ github.repository_owner || 'loft-demos' }}
  BASE_DOMAIN: ${{ inputs.base_domain || 'us.demo.dev' }}
  GIT_BASE_URL: https://github.com
  GIT_PUBLIC_URL: https://github.com
  IMAGE_REPOSITORY_PREFIX: ghcr.io/${{ github.repository_owner || 'loft-demos' }}
  OCI_REGISTRY_HOST: ghcr.io
  SNAPSHOT_OCI_REPOSITORY: ghcr.io/${{ github.repository_owner || 'loft-demos' }}/${{ github.event.repository.name }}
  IMAGE_PULL_SOURCE_SECRET_NAME: ${{ github.repository_owner || 'loft-demos' }}-ghcr-write-pat
  ONEPASSWORD_VAULT: ${{ github.repository_owner || 'loft-demos' }}
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner || 'loft-demos' }}/${{ github.event.repository.name }}-demo-app

jobs:
  replace-text:
    if: ${{ !github.event.repository.is_template && ( github.event_name == 'workflow_dispatch' || contains(github.event.head_commit.message, '[run replace-text]') ) }}
    runs-on: ubuntu-22.04
    permissions:
      packages: write
      contents: write
      actions: write
    steps:
      - name: set-vcluster-name
        id: set-vcluster-name
        run: |
          if [ -n "${{ inputs.vcluster_name }}" ]; then
            echo "VCLUSTER_NAME=${{ inputs.vcluster_name }}" >> "$GITHUB_ENV"
          else
            echo "VCLUSTER_NAME=$(echo "${REPO_NAME%-app}")" >> "$GITHUB_ENV"
          fi
          echo "vCluster name is $VCLUSTER_NAME"
      - uses: actions/checkout@v3
      - uses: flcdrg/replace-multiple-action@v2.0.148
        with:
          files: '**/*.yaml'
          find:  |
            [
              { "find": "{REPLACE_REPO_NAME}", "replace": "${{ env.REPO_NAME }}" },
              { "find": "{REPLACE_ORG_NAME}", "replace": "${{ env.ORG_NAME }}" },
              { "find": "{REPLACE_VCLUSTER_NAME}", "replace": "${{ env.VCLUSTER_NAME }}" },
              { "find": "{REPLACE_BASE_DOMAIN}", "replace": "${{ env.BASE_DOMAIN }}" },
              { "find": "{REPLACE_GIT_BASE_URL}", "replace": "${{ env.GIT_BASE_URL }}" },
              { "find": "{REPLACE_GIT_PUBLIC_URL}", "replace": "${{ env.GIT_PUBLIC_URL }}" },
              { "find": "{REPLACE_IMAGE_REPOSITORY_PREFIX}", "replace": "${{ env.IMAGE_REPOSITORY_PREFIX }}" },
              { "find": "{REPLACE_OCI_REGISTRY_HOST}", "replace": "${{ env.OCI_REGISTRY_HOST }}" },
              { "find": "{REPLACE_SNAPSHOT_OCI_REPOSITORY}", "replace": "${{ env.SNAPSHOT_OCI_REPOSITORY }}" },
              { "find": "{REPLACE_IMAGE_PULL_SOURCE_SECRET_NAME}", "replace": "${{ env.IMAGE_PULL_SOURCE_SECRET_NAME }}" },
              { "find": "{REPLACE_1PASSWORD_VAULT}", "replace": "${{ env.ONEPASSWORD_VAULT }}" }
            ]
          prefix: ''
          suffix: ''
      - uses: stefanzweifel/git-auto-commit-action@v5
      - name: delete-package
        id: delete-package
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.KM_PAT_DELETE_PACKAGES }}
        run: |
          PACKAGE_NAME=${{ github.event.repository.name }}-demo-app

          echo "[INFO] Attempting to delete package: $PACKAGE_NAME"

          if gh api \
            --method DELETE \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            /orgs/${{ env.ORG_NAME }}/packages/container/$PACKAGE_NAME; then
            echo "[INFO] Package deleted successfully."
          else
            EXIT_CODE=$?
            if [[ "$EXIT_CODE" -eq 1 ]]; then
              echo "[WARN] Package '$PACKAGE_NAME' not found. Skipping delete."
            else
              echo "[ERROR] Failed to delete package '$PACKAGE_NAME' (exit code: $EXIT_CODE)"
              exit $EXIT_CODE
            fi
          fi
      - name: Get short SHA
        run: echo "SHORT_SHA=$(echo ${{ github.sha }} | cut -c1-8)" >> $GITHUB_ENV
      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          labels: |
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.title=${{ github.event.repository.name }}
            org.opencontainers.image.vendor=loft.sh
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
          registry: ghcr.io
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./src
          platforms: linux/amd64
          push: true
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache,mode=max
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.SHORT_SHA }}
          labels: ${{ steps.meta.outputs.labels }}
      - name: Disable this workflow
        shell: bash
        run: |
          gh workflow disable -R $GITHUB_REPOSITORY "${{ github.workflow }}"
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
