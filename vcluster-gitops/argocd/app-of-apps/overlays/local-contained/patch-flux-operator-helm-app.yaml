apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: flux-operator-helm-app
  namespace: argocd
spec:
  source:
    helm:
      valuesObject:
        web:
          ingress:
            tls: []
